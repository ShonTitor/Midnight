Space

Comet fib(planet n) -> planet {
	if (n <= 1) {
		return 1
	}
	else {
		planet r1 = fib(n-1);
		planet r2 = fib(n-2);
		planet r = r1+r2;
		return r
	}
}

(planet -> planet) Comet g = fib;
planet r = g(6);
print(r)

EndofSpace